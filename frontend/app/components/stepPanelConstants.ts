/** Max rows/items shown in formatted tables/lists for steps 1–6 (mempool flow). */
export const STEP_TABLE_PREVIEW_LIMIT = 10;

export const stepPanelTableWrapClass = 'border border-line/10 rounded-lg overflow-hidden';

/** Neutral bar above step data tables — keep consistent across panels. */
export const stepPanelTableBarClass =
  'bg-fill-subtle/5 border-b border-line/10 px-3 py-2 text-xs text-fg/70';

export const stepPanelTableFootnoteClass = 'text-fg/50 text-xs text-center mt-2';

export const stepPanelEmptyTextClass = 'text-fg/60 text-sm';
