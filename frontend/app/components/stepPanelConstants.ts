/** Max rows/items shown in formatted tables/lists for steps 1–6 (mempool flow). */
export const STEP_TABLE_PREVIEW_LIMIT = 10;

export const stepPanelTableWrapClass = 'border border-white/10 rounded-lg overflow-hidden';

/** Neutral bar above step data tables — keep consistent across panels. */
export const stepPanelTableBarClass =
  'bg-white/5 border-b border-white/10 px-3 py-2 text-xs text-white/70';

export const stepPanelTableFootnoteClass = 'text-white/50 text-xs text-center mt-2';

export const stepPanelEmptyTextClass = 'text-white/60 text-sm';
